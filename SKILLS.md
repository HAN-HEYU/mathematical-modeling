# 本地数学建模 Skills

本项目可以配合本机安装的 Codex Skills 使用。Skill 是 Codex 的任务工作流，
不是 Python 包，也不会通过 `requirements.txt` 安装。不同电脑或 Codex 环境中的
可用 Skill 可能不同，开始工作前应以当前会话显示的 Skills 列表为准。

## 如何调用

在向 Codex 提出任务时写出 Skill 名称即可，推荐使用 `$SkillName` 明确指定：

```text
$math-modeling 读取 data/raw 中的赛题与附件，完成整题建模流程。
$建模手 只分析题目、拆分子问题并选择模型，暂时不要写代码。
$编程手 根据现有模型说明实现 q1，运行测试并输出可复现结果。
$科研可视化工具 根据 results/q1 的数据生成论文候选图。
```

同一任务可以指定多个 Skill。Codex 会先读取各 Skill 的说明，再按它们的约束
决定执行顺序。未明确指定时，如果任务与某个 Skill 的用途明显匹配，Codex 也
可能自动启用它。

## 建模主流程

| Skill | 适用场景 | 常见产物 |
|---|---|---|
| `$math-modeling` | 完整数学建模流程或跨阶段任务 | 分析、代码、结果、图表和论文草稿 |
| `$建模手` | 读题、拆题、假设、模型选择与算法设计 | 题目分析报告、术语表格 |
| `$编程手` | Python/MATLAB 实现、运行、验证和复现 | 代码、结果表、图表、复现清单 |
| `$论文手` | 根据真实模型结果撰写数学建模论文 | Word 论文；明确要求时同时生成 LaTeX |

完整流程通常按“建模手 -> 编程手 -> 论文手”推进。只需要其中一个阶段时可直接
调用对应 Skill，但编程阶段应有可执行的模型说明，论文阶段应有真实运行结果。

## 文件与论文工具

| Skill | 适用场景 |
|---|---|
| `$pdf` | 读取赛题 PDF、提取文字或表格、拆分合并与 OCR |
| `$Excel工具` | 读取、创建、修改和校验 XLSX 附件或结果表 |
| `$DOCX工具` | 创建、编辑和校验 Word 数学建模论文 |
| `$LaTeX工具` | 创建、编译和校验 LaTeX 论文项目 |
| `$双引擎论文搜索` | 使用两个真实数据源检索并交叉核验论文 |
| `$科研可视化工具` | 数据剖析、选图、出版级绘图和图表审计 |
| `$jupyter-notebook` | 创建或维护可复现的探索与实验 Notebook |

涉及具体文件格式时，最好同时给出仓库内路径。例如：

```text
$pdf 读取 data/raw/problem.pdf，提取题目、约束和符号定义。
$Excel工具 检查 data/raw/attachment.xlsx 的工作表、字段、缺失值和单位。
$DOCX工具 根据 paper/abstract.md 和已验证结果维护最终论文草稿。
```

## 算法与分析工具

| Skill | 适用场景 |
|---|---|
| `$sympy` | 符号代数、方程求解、微积分和公式化简 |
| `$scikit-learn` | 分类、回归、聚类、降维、流水线和模型评估 |
| `$statsmodels` | OLS、GLM、时间序列及带统计推断的模型诊断 |
| `$statistical-analysis` | 检验选择、假设检查、效应量、功效和结果表述 |
| `$pymoo` | 多目标优化、约束处理和 Pareto 前沿 |
| `$simpy` | 离散事件仿真、资源与排队过程 |
| `$networkx` | 图论、复杂网络、路径、中心性和社区分析 |
| `$aeon` | 时间序列预测、分类、聚类与异常检测 |
| `$exploratory-data-analysis` | 本地科学数据的有界探索、质量与泄漏检查 |

这些专项 Skill 应按题目结构选用，不需要在每道题中全部调用。例如，多目标规划
可组合 `$建模手` 与 `$pymoo`，时间序列预测可组合 `$编程手` 与 `$aeon`。

## 使用约定

- 原始附件保持在 `data/raw/`，原则上只读。
- Skill 生成的代码、结果、图表和论文均需人工复核。
- 数值结论必须来自实际运行，不能把示例输出写成正式结果。
- 论文格式、页数和学术诚信要求以目标竞赛当届官方规则为准。
- 如果 Skill 在当前环境不可用，应先安装或改用已确认可用的工作流，不要假定
  仓库中的这份清单等于运行环境中的实际清单。
